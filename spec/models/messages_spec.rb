require 'rails_helper'

describe Message do

  it "should return encrypted message" do
    # message = Message.new(message: 'Some new message', delaydelete: true)
    # attr_encrypted message.message
    # expect(message.message).to eq('Some new message')
  end

end
